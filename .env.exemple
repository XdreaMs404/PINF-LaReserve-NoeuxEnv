-- 1) Comptes de départ (pour pouvoir se connecter à l'admin)
-- MDP (à noter) :
-- admin@pinf.local    => Admin123!
-- redacteur@pinf.local=> Redac123!
-- lecteur@pinf.local  => Lecteur123!
-- (hash bcrypt compatible PHP password_verify)

# Configuration Base de Données
DB_HOST=localhost
DB_NAME=pinf_syra
DB_USER=ErineD
DB_PASS=
DB_CHARSET=utf8mb4

# Configuration Générale
APP_ENV=local
APP_DEBUG=true